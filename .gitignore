/target/
**/*.rs.bk

# Cargo.lock is committed (this workspace's top-level product is the
# `quetzalcoatl` binary, and binaries should pin their dependency versions).

# Real credentials -- see quetzalcoatl.example.toml for the placeholder
# version. Ignored by exact name, not a broad glob, so the example file
# stays tracked.
quetzalcoatl.toml

**/.DS_Store
.vscode/
.idea/
